#include "main.h"

/**
 * _strlen - fucntion that returns the length of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int len = 0;

	while (*s != '\0')
	{
		lem++;
		s++;
	}

	return (len);
}
